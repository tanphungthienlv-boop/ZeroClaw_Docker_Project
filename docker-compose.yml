version: '3.8'

services:
  zeroclaw:
    build: .
    container_name: zeroclaw_bot
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./config:/root/.zeroclaw
    environment:
      - RUST_LOG=info
